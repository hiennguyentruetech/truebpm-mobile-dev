/// Enum for CoreSelect types
enum CoreSelectType {
  select,    // Simple dropdown selection only
  dropdown,  // Dropdown that opens popup with detailed view
  multiple,  // Multiple selection with popup and checkboxes
}
