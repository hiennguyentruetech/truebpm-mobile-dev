import 'package:flutter/material.dart';

/// App color palette
class AppColors {
  // Primary colors
  static const primary = Color(0xFF2A75BC);
  static const primaryLight = Color(0xFF5E9FEF);
  static const primaryDark = Color(0xFF004D8C);

  // Accent colors
  static const accent = Color(0xFFFFA000);
  static const accentLight = Color(0xFFFFD149);
  static const accentDark = Color(0xFFC67100);

  // Background colors
  static const background = Color(0xFFF5F5F5);
  static const backgroundDark = Color(0xFF121212);

  // Surface colors
  static const surface = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF1E1E1E);

  // Text colors
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color.fromARGB(255, 57, 57, 57);
  static const textPrimaryDark = Color(0xFFE0E0E0);
  static const textSecondaryDark = Color(0xFFAEAEAE);

  // Error colors
  static const error = Color(0xFFB00020);
  static const errorDark = Color(0xFFCF6679);

  // Success colors
  static const success = Color(0xFF4CAF50);
  static const successDark = Color(0xFF66BB6A);

  // Warning colors
  static const warning = Color(0xFFFFC107);
  static const warningDark = Color(0xFFFFD54F);

  // Info colors
  static const info = Color(0xFF2196F3);
  static const infoDark = Color(0xFF64B5F6);

  // Dialog semantic colors (used by AppThemedDialog)
  static const dialogSuccess = Color(0xFF2E7D32);
  static const dialogSuccessDark = Color(0xFF1B5E20);
  // Keep warning in a deep yellow family (avoid orange tone)
  static const dialogWarning = Color(0xFFC0A200);
  static const dialogWarningDark = Color(0xFF8C7600);
  static const dialogError = Color(0xFFC62828);
  static const dialogErrorDark = Color(0xFF8E0000);
  static const dialogInfo = Color(0xFF1565C0);
  static const dialogInfoDark = Color(0xFF0D47A1);

  // Divider colors
  static const divider = Color(0xFFBDBDBD);
  static const dividerDark = Color(0xFF424242);
}
